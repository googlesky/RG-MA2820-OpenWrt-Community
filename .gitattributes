# Patch files contain their own diff markers; a leading space before a tab is
# valid context syntax in the nested diff rather than source indentation.
*.patch whitespace=-trailing-space,-space-before-tab
